VITE_API_BASE_URL=http://localhost:4000

# Use in-memory mock data instead of API calls (no backend required)
VITE_MOCK_MODE=true

# Dev-only: prefill login form (ignored in production builds)
# Dev form prefill (mock mode staff logins: administration, operations, technician, doctor)
VITE_DEV_LOGIN_USERNAME=administration
VITE_DEV_LOGIN_PASSWORD=Admin@123
