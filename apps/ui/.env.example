# Livotale UI — environment

VITE_API_BASE_URL=http://localhost:3008/api/v1

# When using docker nginx gateway on port 3008 (see docker-compose nginx service)
# VITE_HMR_CLIENT_PORT=3008

# External integrations: dummy (local stubs) or live (provider credentials on API)
VITE_EXTERNAL_SERVICES_MODE=dummy
